#ifndef TYPES_H
#define TYPES_H

typedef struct tile Tile;
typedef struct piece Piece;
typedef enum color Color;
typedef enum type Type;
typedef int move_t[2];
typedef unsigned int uint;

#endif /* TYPES_H */
